# Ignore Rules

Preserve these without translation:

- YAML front matter (entire block)
- Code blocks (fenced or indented)
- Inline code spans
- Link URLs and image URLs
- Markdown syntax markers (headings, lists, tables, emphasis)

Translate only human-readable text content outside the items above.
