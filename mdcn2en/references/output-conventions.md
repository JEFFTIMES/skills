# Output Conventions

- Input: Chinese Markdown file with `.md` extension.
- Output: sibling file in the same directory.
- Naming:
  - `foo.md` -> `foo.en.md`
  - If a language suffix exists (e.g., `foo.zh.md`), replace with `.en.md`.
- Do not modify YAML front matter.
